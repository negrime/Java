package com.company;

public class CalculateLite {
    public static float Calculate(String expression) {
        float result = 0;
        for (int i = 0; i < expression.length() / 2; i++) {
            if ((expression.charAt(i + 1)) == '+')
            {

            }
        }
        return 1;
    }
}
